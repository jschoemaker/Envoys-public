{
  "name": "envoys",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter envoys-api dev",
    "build": "pnpm --filter '*' build",
    "install:all": "pnpm install"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
